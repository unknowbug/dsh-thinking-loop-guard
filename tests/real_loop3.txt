实际上，让我用 Java 探针本身找结构。BlockProbe 有结构相关吗？

让我看 BlockProbe 是否有结构定位功能。

考虑到这是大工程，且需要找含结构区域，我应该先找结构位置。

让我先确认默认种子 -8248 是否有结构（用 Java 探针或 vanilla 数据）。

实际上，考虑到这是重活，且需要找含结构区域，我应该先找结构位置。

让我先找结构位置。用 vanilla 数据（structure 定位）。

考虑到这是大工程，让我先找结构位置。vanilla 结构（村庄）在特定 biome + 位置。

让我写一个探针找结构位置？但结构定位需要 vanilla 结构生成。

实际上，让我用 Java 探针本身找结构。BlockProbe 有结构相关吗？

让我看 BlockProbe 是否有结构定位功能。

考虑到这是大工程，且需要找含结构区域，我应该先找结构位置。

让我先确认默认种子 -8248 是否有结构（用 Java 探针或 vanilla 数据）。

实际上，考虑到这是重活，且需要找含结构区域，我应该先找结构位置。

让我先找结构位置。用 vanilla 数据（structure 定位）。

考虑到这是大工程，让我先找结构位置。vanilla 结构（村庄）在特定 biome + 位置。

让我写一个探针找结构位置？但结构定位需要 vanilla 结构生成。

实际上，让我用 Java 探针本身找结构。BlockProbe 有结构相关吗？

让我看 BlockProbe 是否有结构定位功能。

考虑到这是大工程，且需要找含结构区域，我应该先找结构位置。

让我先确认默认种子 -8248 是否有结构（用 Java 探针或 vanilla 数据）。

实际上，考虑到这是重活，且需要找含结构区域，我应该先找结构位置。

让我先找结构位置。用 vanilla 数据（structure 定位）。

考虑到这是大工程，让我先找结构位置。vanilla 结构（村庄）在特定 biome + 位置。

让我写一个探针找结构位置？但结构定位需要 vanilla 结构生成。

实际上，让我用 Java 探针本身找结构。
